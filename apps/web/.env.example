# Web app (Next.js)
# Proxy: browser calls same-origin /api/users/*, Next forwards to user-service.
#
# In development, if USER_SERVICE_URL is missing, the proxy defaults to http://localhost:4001.
# Recommended: set it explicitly to avoid surprises.
USER_SERVICE_URL=http://localhost:4001
