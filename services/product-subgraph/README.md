# product-subgraph

Federation subgraph cho catalog (Milestone 0: chỉ có `ping` + demo `Product @key`).

## Dev
- `pnpm --filter product-subgraph install`
- `pnpm --filter product-subgraph dev`

Default port: `4002`.
